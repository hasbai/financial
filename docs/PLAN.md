# 实施规划

2026-09-13 更新。以用户后续明确要求为准：单人个人财务系统，保持简单。原先多账本、成员、独立草稿、审计和额外 schema 的规划已撤销。

## 范围

1. Auth0 北极小站登录，使用 Auth0 superadmin 角色授权；人民币。
2. 总览：资产负债、现金流、损益、趋势和待补录提示。
3. 流水：手机卡片、搜索/筛选/分页、手工新增、补录、科目匹配和分录拆分。
4. 科目设置：新增、编辑原有字段；现金范围直接按既有子类识别。
5. Svelte 5 SPA 静态部署 Cloudflare Worker financial，域名 financial.hasbai.xyz，GitHub hasbai/financial。

## 实现原则

保持现有三张表和全部原字段不变。视图直接计算完整性、三表报表和现金流；保存函数一次提交交易及分录。前端防止连续点击，已有记录复用 updated_at 处理编辑冲突，不加 version。

退款在同笔交易内追加反向分录。历史完整成功/退款记录依原分录计入已记录范围报表，不批量改写来源状态，不猜测合并。现金范围为“资产 / 现金及等价物”。

## 顺序与验收

| 阶段 | 验收 |
| --- | --- |
| 接入 | Auth0 刷新、真实登录、Data API 本人访问、无效 token 拒绝 |
| 数据 | 三表原数据/ID 保留，SQL 金额和报表用例、权限、保存回滚通过 |
| 页面 | 总览/流水/补录/科目设置可用；移动布局、错误保留输入、返回导航 |
| 发布 | 构建、Worker SPA 回退、目标域名、生产配置及登录验收 |

实际执行证据与未完成事项见 [PROGRESS](PROGRESS.md)。不将开发完成等同于生产发布完成。

## 2026-09-15 余额历史与每日统计

- 当前余额读取现有 balance，新增 balance_history 物化视图；维护时全量计算日历史，不设置定时任务。
- 首页仅统计；现金、余额、损益面板按日展示并下钻到当天记录；现金默认所选月份。
- 保留三张原表及 ID、用户余额规则和本人权限，先隔离分支验证，再分别记录数据库/API/前端交付状态。

## 2026-09-15 角色授权与直接读取

后续要求覆盖旧的本人 subject / SQL 身份检查与单 GET 首页方案。移除 is_owner、read_* 和页面包装视图；Auth0 顶层 role 经 Data API `.role` 映射 PostgreSQL superadmin。前端直接读取已有报表，以 Decimal 组装页面数据。先隔离验证，生产数据库和前端配套切换。

## 2026-09-15 iOS 优先录入与 PWA

实施范围：移动端滚动与安全区、键盘可见视口、按type/subtype/name二级选择、收入扣款/多分类/多账户/转账业务表单，以及仅缓存公开资源的PWA。不改三表字段、历史ID或报表口径。交付按类型检查、组件/金额/PWA测试、构建、真实API、提交推送与Cloudflare自动部署逐层核验；不使用浏览器、不声明真机体验已验收。
