# 参考 UI 与文案排查

2026-09-14。以用户三张参考图及“首页现金流改为通栏”“禁止解释性小字”的修订为依据。检查范围：登录外壳、总览四个面板、现金流图表/明细、流水搜索/筛选/分页、补录简单/复杂表单/退款、科目页面、共用空状态及异常反馈。

| 原差异 | 修复后的交互 | 实现入口 |
| --- | --- | --- |
| 待补录与期初口径横幅占据首页顶部 | 铃铛入口、状态圆点与无障碍计数 | Overview |
| 净资产、资产负债卡底部余额说明 | 只保留标题、金额、方向入口；主卡提供显隐与真实余额曲线 | Overview / BalanceSparkline |
| 总览堆叠趋势、余额、数据质量说明 | 四项分段导航；明细进入对应面板 | Overview |
| 现金流底部大段口径、分类说明 | 通栏金额/双柱图，明细和流水入口 | Cashflow / CashBars |
| 图表明细额外解释日期边界 | 时间列、金额列；业务规则保存在 DOMAIN | CashBars |
| 流水没有月度汇总 | 收入、支出、净流入汇总与真实逐日迷你图 | Transactions / MiniBars |
| 搜索框、筛选和卡片层次偏离参考 | 胶囊搜索、收支分段筛选、日期分组、可移除筛选项 | Transactions |
| 所有交易统一图标且金额无方向 | 真实科目类别图标、正负号、中性转账、缺失状态徽标 | TransactionIcon / presentation |
| 补录使用长分录表单和借贷教学文字 | 简单收支改为分类/账户等紧凑操作行，复杂分录保留完整表单 | EditorForm / editor |
| 无可执行的科目建议区域 | 从同商户历史完整交易取科目，用户一键应用，不复制分录 ID | EditorForm |
| 交易编号、更新说明、来源字段占主界面 | 摘要卡；来源与分录明细进入操作菜单 | EditorForm |
| 保存按钮布局不一致 | 底部“稍后处理 / 保存补录”，返回与更多操作置顶 | EditorForm |
| 科目现金范围横幅、登录权限小字、空状态教学 | 科目徽标、登录按钮、空状态操作入口 | Accounts / Shell / Empty |
| 顶部重复品牌、工具和底部导航冗长 | 标题归页面，工具入应用菜单；底部短标签与圆形新增按钮 | Shell |
| 结束日期需要“不含”说明且 UTC 日期可能错位 | 北京时间自然日控件，结束日转换为次日半开边界 | Transactions |

## 数据与功能边界

不复制参考图的虚构金额、百分比或品牌图标。没有数据接口的环比不伪造；AI 建议改为可追溯的历史科目建议。预算、删除和额外管理功能受现有三表、单人使用及原始记录保留约束，不加入虚假入口。现金流默认所选月份，保留已录入未来交易的30天范围。2026-09-15 首页改为只展示统计，余额/现金/损益面板提供逐日下钻；隐藏金额时同时隐藏图表及每日表格。

## 验证证据

- 组件交互测试验证首页面板切换、待补录入口、金额显隐、未来日期下钻、空与失败状态。
- 流水测试验证汇总数值、金额符号、搜索/筛选/分页和北京时间结束日期转换。
- 补录测试验证紧凑字段、双边金额同步、切换收支、历史建议手动应用、复杂分录入口、同笔退款与全部原分录 ID、失败输入保留、重复保存保护及返回守卫。
- 共用文案回归扫描覆盖所有业务页面、共用组件和外壳，拦截本次清理的说明性文案；AGENTS.md 固化新约束。
- 深浅语义色、移动列宽、菜单/字段焦点、滚动与安全区按源码检查。未使用浏览器，因此不将自动化 DOM 或源码检查称为浏览器视觉验收。

最终命令结果记录在 PROGRESS.md。

2026-09-18 用户要求覆盖此前删除范围限制：已有交易统一“修改交易”，支持单笔删除及分录级联；科目支持五位编号编辑、无引用删除和两级折叠，科目页加号仅新增科目。

2026-09-19 用户进一步确认交易编辑页以当前交易名称作为标题，删除重复摘要卡和历史科目建议；状态、渠道、流水号常驻显示，转账文案改为“划转”，业务细项改为无嵌套卡片的紧凑列表，选择项按科目 ID 排序。本条覆盖 2026-09-18 的固定“修改交易”标题及早期摘要、建议设计。


## 2026-09-20 移动端逐页实查与修复

使用 Chrome DevTools MCP，在已登录的生产页面以 390×844、375×812 的触控视口检查；科目编辑另缩至375×420。检查浅色和深色、页面滚动、报表切换和日期下钻、流水筛选/搜索、交易类型/分类/账户弹层、退款入口、完整分录、科目两级展开及空搜索。生产仅浏览和未保存的编辑，未提交交易、退款或删除。以下记录不包含个人账本截图和金额，避免进入公共仓库。

| 问题与观察 | 修复方案 | 验收重点 |
| --- | --- | --- |
| 损益 SVG 固定800单位宽，在手机上文字与曲线缩到不可辨认，绘图区上下大量空白 | 按实际容器宽绘制，固定可读字号，增加金额刻度与零线，沿用真实逐日数据 | 手机字号、负值范围、日期下钻、隐藏金额、深色 |
| 现金流入口独占一行，卡片标题/期间选择层次松散 | 每日明细入口归入卡片标题，手机期间选择占满一行，缩短间距 | 总览/现金流面板、未来空态、入口可触达 |
| 资产负债筛选和月份分别占行，加载时月份消失 | 两者放同一上下文行，窄屏自然换行，月份不随请求状态卸载 | 375px与iPhone SE、加载/错误 |
| 流水汇总三列垂直居中；只有净流入含小图，导致标题和数字不齐 | 三列顶部对齐，小图限制在本列宽度内 | 月度汇总正常/隐藏金额 |
| 展开流水筛选占据正文并把交易推到屏幕外；渠道和科目条件收起后不可见 | 手机使用有固定操作区的底部筛选面板，嵌套选择完成后回到筛选；正文显示实际条件与可逐项清除入口 | 嵌套弹层、短视口、焦点返回、URL与清空行为 |
| 日期下钻只显示“日期”，不知道筛到哪一天；新列表可能继承来源页滚动 | 显示北京时间自然日范围，等待列表可用后恢复对应位置，无已保存位置则回到顶部 | 半开区间不变、日期标签、下钻到顶部 |
| 流水直接渠道显示内部枚举direct，转账文案与编辑器不一致 | 显示直接交易/划转 | 不改变保存值或筛选协议 |
| 收入空扣款区及每个拆分按钮单独占行；多分录的选择、清除、金额和删除挤在一行 | 添加/拆分放在分区标题右侧，多分录选择与金额分行 | 类型转换、拆分、分配、48px触控、长科目名 |
| 科目编辑在短视口下标题和操作随字段滚走，三个纵排操作占高 | 标题和取消/保存固定，字段独立滚动，删除放标题栏；关闭归还原入口焦点 | 深色375×420、失败输入保留、嵌套类型选择 |
| 深色原生日期图标仍是黑色；科目空搜索没有恢复入口 | 明确color-scheme，空搜索提供清空搜索；科目选择支持ID搜索 | 深色日期、空态、键盘选择 |

设置页科目入口、分类/账户面板的底部定位与关闭、退款面板及完整分录保存栏本次实查可用，保留其业务结构。浮动加号遵循既有参考设计，长列表末尾保留滚动余量。本轮不声称已覆盖真机软键盘、PWA系统返回手势或所有浏览器组合；异常加载/失败/保存/删除由GitHub Actions合成数据验证。新增报表面板、筛选、拆分收入和短视口科目弹层的视觉证据登记在visual-coverage.json。


候选复核补充：320px iPhone SE的三列统计把小数末位拆到下一行，改为该窄屏下逐行对齐标签与完整金额。短屏按钮可见性出现WebKit亚像素交叉比例0.999902；触控门禁增加四边相对VisualViewport的0.5px容差检查，并将交叉比例设为0.999，仍要求中心命中与至少48px，不接受实际遮挡。


第二轮候选：基础检查105项通过；短屏表单通过固定操作检查后，进一步复现程序化输入聚焦到屏外字段未滚入可视区。给表单字段滚动区增加焦点/尺寸变化跟随，只调整内部scrollTop，保持标题和操作区固定。完整截图复核同时收紧320px首页资产负债双卡的图标、标签及金额，避免标题和小数拆行。


上线复核：真实损益日序列较密时，原等间隔抽样加最后一天造成末两个日期重叠。改用绘图区宽度计算可容纳的标签段数，再按全序列等距选择日期；补16个交易日含负值的375px回归，要求相邻标签至少8px间距。


## 2026-09-20 内置浏览器移动端美观复查

本轮使用 Codex 内置浏览器，390×844 手机视口直接浏览线上个人账本。检查了总览、资产/负债筛选、现金流所选月份/未来空态、损益及逐日下钻、流水与筛选弹层/嵌套科目选择、已有支出/新增收入扣款/划转、交易操作/退款/完整分录、设置、科目两级展开/编辑/类型弹层/空搜索。仅操作浏览和未保存表单，未保存、退款或删除生产记录。个人数据和线上截图不提交公共仓库。

美观修复方案：

1. 移动端新增圆形加号放入底栏右侧独立位置，导航保留三个入口。图表、科目金额和流水不再被悬浮按钮盖住；同步缩短页面底部空白。
2. 手机卡片统一20px圆角和轻阴影；净资产主卡使用轻微资产蓝底色，金额显隐靠右，主数字与曲线保持层次。
3. 报表月份行上下间距收紧；现金流的查看流水入口与净流入标签同排，期间选择采用浅底选中态，减少多个深色按钮竞争。
4. 应用/交易操作菜单统一紧凑间距、右对齐和屏幕边缘留白；科目选择搜索框左右内缩16px，与选项对齐；表单弹层输入圆角统一14px。

自动化沿用现有正常、加载、空、错误、隐藏金额、深色、短屏及键盘场景；增加移动底栏新增按钮和三项导航不重叠的行为断言，并登记覆盖清单。所有自动化交由GitHub Actions，本地仅格式化和git diff --check。候选、普通CI及部署结论在交付结果中单独列出。


候选复核：第一轮CI全部通过，但实际截图显示低比例OKLCH混色产生偏粉底色，且窄屏净资产曲线穿过金额末位。改为OKLab混色，并在主卡底部为趋势单独留出44px区域，金额与曲线互不重叠；重新生成对应新提交的候选，不接受首轮截图。
