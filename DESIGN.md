# 界面与交互

以用户提供的三张手机 UI 图为基准，使用 Svelte 5、Bits UI、shadcn-svelte、Tailwind CSS 4 和 Lucide。浅蓝灰背景、白色圆角卡片、深石板色主按钮；资产蓝、现金流入绿、流出玫红、损益橙。中文系统字体，金额等宽数字；深色模式有独立语义色。

## 文案约束

禁止常驻解释性小字、说明横幅、口径免责声明和操作教学，也不把这些说明转移到 tooltip 或说明弹窗。约束已写入 AGENTS.md。日期、金额、图例、科目、字段标签及简短操作状态属于业务信息。口径仅写入 DOMAIN.md，界面使用选中筛选、状态、计数、字段校验、空状态操作和明细入口。

## 总览

- 标题为“个人财务”；搜索进入流水，铃铛的待补录入口有状态圆点与可访问计数。
- 总览 / 资产负债 / 现金流量 / 损益切换。总览仅保留净资产主卡、资产/负债双卡、通栏现金流、横排损益；余额表和损益趋势进入各自面板。
- 净资产主卡提供金额显隐按钮及基于 SQL 截止时点余额的曲线；不伪造单调增长曲线或涨跌幅。
- 首页只展示统计；逐日余额、现金流、损益在各自面板提供可键盘访问的日期链接，下钻至当天流水。

## 现金流

默认展示所选月份净流入，保留未来30天切换；流入/流出按日展示柱状图，现金面板展示每日金额，点击日期进入同范围流水。手机为上下布局，桌面为左右布局，整张卡通栏。图例、金额轴、日期和明细表保留，所有说明段落移除。

未来区间为页面打开时点起连续30天，合计与每日统计从 cashflow 视图在 SQL 内计算，前端仅处理日期与绘图。隐藏金额时不渲染图形或金额明细；无数据、缺现金账户和错误分别给出空状态/设置入口/重试操作。

## 流水

月度收支汇总、月份选择、胶囊搜索框、全部/支出/收入/待补录/待匹配筛选和按日卡片。月度汇总独立标明月份，列表保留已有 URL 筛选和游标分页；选择月份会同步列表起止日期。已入账、现金和日期过滤通过可操作筛选项呈现。

卡片显示真实科目类别图标、商户、时间、带符号金额与分类/缺失状态。支出为负号，收入/退款为正号，转账保持中性。金额未知保留待补录，不能当作零。汇总迷你图来自 SQL 损益逐日数据。

日期输入以北京时间显示，结束日期控件包含所选日，提交时转为 SQL 半开区间；不再用说明括号解释日期边界。

## 补录

手机全屏，桌面居中 Dialog。顶部返回、标题与操作菜单；交易摘要卡显示真实商户、时间、渠道、金额、状态和原备注。

简单收支直接编辑类型、分类、账户、金额、对方、时间和备注。金额输入同步借贷，类型变更维护相反方向，始终保留原分录 ID。缺分类/账户显示暖色选择控件。相同商户的历史完整交易可提供科目建议，必须手动应用；不展示 AI 字样或虚构置信度。

转账、退款、多分录、原有不平衡记录继续进入完整分录编辑。分录明细、交易来源、补记退款和保存并下一笔从操作菜单进入。底部为“稍后处理 / 保存补录”，新增为“保存交易”。编辑时展示金额，退出后沿用全局显隐。

失败保留输入；重复保存保护、updated_at 冲突、返回/刷新未保存保护保持。网络结果不明确时禁用保存，提供核对流水入口。退款继续加入同一 transaction，原分录保留。

## 应用外壳与科目

移动底栏为总览 / 流水 / 科目，圆形“记一笔”按钮；桌面侧栏。金额显隐、外观和退出进入应用菜单，避免顶端重复标题和多排工具。科目页去除现金范围横幅，以科目标签表示现金范围。

参考图中的预算、批量删除、未接入的通知、示例涨跌幅及 AI 置信度不生成占位控件，遵循既有单人三表和保留历史记录边界。

## 可访问性与验证

按钮/字段至少48px触控，焦点环、明确的控件名称、Bits UI 焦点归还与键盘选择；固定底栏和弹窗操作区保留安全区。金额隐藏连同图形和表格隐藏；减少动效由主题处理。

按用户既有要求不使用浏览器。自动化检查真实组件 DOM、交互、字段映射、文案回归、类型与构建，不能声称像素或真机验收。参考差异排查记录见 docs/UI-AUDIT.md，验证状态见 docs/PROGRESS.md。
